---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: nodetaintrules.nodeops.node.io
spec:
  group: nodeops.node.io
  names:
    kind: NodeTaintRule
    listKind: NodeTaintRuleList
    plural: nodetaintrules
    singular: nodetaintrule
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: NodeTaintRule is the Schema for the nodetaintrules API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of NodeTaintRule
            properties:
              nodeSelector:
                additionalProperties:
                  type: string
                description: 节点标签筛选，只对匹配标签的node生效
                type: object
              taint:
                description: 需要添加的污点
                properties:
                  effect:
                    description: |-
                      Required. The effect of the taint on pods
                      that do not tolerate the taint.
                      Valid effects are NoSchedule, PreferNoSchedule and NoExecute.
                    type: string
                  key:
                    description: Required. The taint key to be applied to a node.
                    type: string
                  timeAdded:
                    description: TimeAdded represents the time at which the taint
                      was added.
                    format: date-time
                    type: string
                  value:
                    description: The taint value corresponding to the taint key.
                    type: string
                required:
                - effect
                - key
                type: object
              threshold:
                description: 阈值配置
                properties:
                  cpuUsagePercent:
                    description: CPU使用率阈值百分比
                    type: integer
                  diskUsagePercent:
                    description: 磁盘使用率阈值百分比，超过就打污点
                    type: integer
                  memUsagePercent:
                    description: 内存使用率阈值百分比
                    type: integer
                required:
                - cpuUsagePercent
                - diskUsagePercent
                - memUsagePercent
                type: object
            required:
            - taint
            - threshold
            type: object
          status:
            description: status defines the observed state of NodeTaintRule
            properties:
              affectedNodes:
                description: |-
                  conditions represent the current state of the NodeTaintRule resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                  记录已经被打上污点的节点列表
                items:
                  type: string
                type: array
              lastCheckTime:
                description: 最近一次检测时间
                format: date-time
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
